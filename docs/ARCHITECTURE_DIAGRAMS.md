# Authentication System Architecture

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React + Vite)                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │              React Components & Pages                          │    │
│  ├─────────────────────────────────────────────────────────────┐  │    │
│  │  Login.jsx │ Register.jsx │ UserProfile.jsx │ NavBar.jsx  │  │    │
│  ├─────────────────────────────────────────────────────────────┘  │    │
│  │                          ▲                                      │    │
│  │                          │                                      │    │
│  └──────────────────────────┼──────────────────────────────────────┘    │
│                             │                                           │
│                  useAuth() hook (custom)                                │
│                             │                                           │
│  ┌──────────────────────────▼──────────────────────────────────────┐   │
│  │              Zustand Auth Store                                 │   │
│  ├───────────────────────────────────────────────────────────────┐ │   │
│  │  State:                      Methods:                         │ │   │
│  │  • user (object)             • setLogin()                     │ │   │
│  │  • token (JWT)               • setLogout()                    │ │   │
│  │  • isAuthenticated (bool)    • updateProfile()               │ │   │
│  │  • isLoading (bool)          • startAutoLogout()             │ │   │
│  │  • error (string)            • refreshSession()              │ │   │
│  │  • autoLogoutTimer           • setError()                    │ │   │
│  │                                                               │ │   │
│  │  📦 Persisted to localStorage via persist middleware         │ │   │
│  └───────────────────────────────────┬───────────────────────────┘ │   │
│                                       │                              │   │
│  ┌────────────────────────────────────▼───────────────────────────┐ │   │
│  │           Auth Service (authService.js)                        │ │   │
│  ├────────────────────────────────────────────────────────────────┤ │   │
│  │  • register(data)     • updateProfile(data)                    │ │   │
│  │  • login(credentials) • forgotPassword(email)                  │ │   │
│  │  • logout()           • resetPassword(token, pass)             │ │   │
│  └────────────────────────────────────┬───────────────────────────┘ │   │
│                                       │                              │   │
│  ┌────────────────────────────────────▼───────────────────────────┐ │   │
│  │         Axios Instance (api.js)                               │ │   │
│  ├────────────────────────────────────────────────────────────────┤ │   │
│  │  Interceptors:                                                 │ │   │
│  │  • Request: Adds Authorization header with token              │ │   │
│  │  • Response: Handles 401 errors, refreshes session            │ │   │
│  └────────────────────────────────────┬───────────────────────────┘ │   │
│                                       │                              │   │
│  ┌────────────────────────────────────▼───────────────────────────┐ │   │
│  │       Protected Route Component                                │ │   │
│  ├────────────────────────────────────────────────────────────────┤ │   │
│  │  • Checks: isAuthenticated && user.role === requiredRole       │ │   │
│  │  • Actions: Render component OR redirect to /login             │ │   │
│  └────────────────────────────────────────────────────────────────┘ │   │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────▼──────────────────┐
                    │   HTTP Requests (Axios)           │
                    │   Authorization: Bearer {token}   │
                    └─────────────────┬──────────────────┘
                                      │
┌─────────────────────────────────────────────────────────────────────────┐
│                        BACKEND (Express.js)                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │              Auth Routes                                       │    │
│  ├─────────────────────────────────────────────────────────────┐  │    │
│  │ POST   /api/auth/register                                 │  │    │
│  │ POST   /api/auth/login                                    │  │    │
│  │ GET    /api/auth/me        (protected)                    │  │    │
│  │ POST   /api/auth/logout    (protected)                    │  │    │
│  │ PUT    /api/auth/profile   (protected)                    │  │    │
│  │ POST   /api/auth/forgot-password                          │  │    │
│  │ POST   /api/auth/reset-password                           │  │    │
│  └─────────────────────────────────────────────────────────────┘  │    │
│                          ▲                                          │    │
│                          │                                          │    │
│  ┌──────────────────────┼───────────────────────────────────────┐  │    │
│  │ Auth Controller      │                                      │  │    │
│  ├──────────────────────┼───────────────────────────────────────┤  │    │
│  │ • register()         │  JWT Secret: VERY_SECRET_KEY         │  │    │
│  │ • login()            │  Token Expiry: 1 hour                │  │    │
│  │ • getMe()            │                                       │  │    │
│  │ • logout()           │                                       │  │    │
│  │ • updateProfile()    │                                       │  │    │
│  │ • forgotPassword()   │                                       │  │    │
│  │ • resetPassword()    │                                       │  │    │
│  └──────────────────────┼───────────────────────────────────────┘  │    │
│                         │                                           │    │
│  ┌──────────────────────┼───────────────────────────────────────┐  │    │
│  │ Auth Middleware      │                                      │  │    │
│  ├──────────────────────┼───────────────────────────────────────┤  │    │
│  │ • protect()          Verifies JWT token                     │  │    │
│  │ • authorize()        Checks user role                       │  │    │
│  └──────────────────────┼───────────────────────────────────────┘  │    │
│                         │                                           │    │
│  ┌──────────────────────▼───────────────────────────────────────┐  │    │
│  │              User Model                                      │  │    │
│  ├───────────────────────────────────────────────────────────┐  │  │    │
│  │  Fields:                                                 │  │  │    │
│  │  • _id                                                   │  │  │    │
│  │  • fullname                                              │  │  │    │
│  │  • username (unique)                                     │  │  │    │
│  │  • email (unique)                                        │  │  │    │
│  │  • password (bcrypt hashed)                              │  │  │    │
│  │  • role ('user' or 'admin')                              │  │  │    │
│  │  • createdAt                                             │  │  │    │
│  │  • updatedAt                                             │  │  │    │
│  └───────────────────────────────────────────────────────────┘  │  │    │
│                         │                                        │  │    │
│                         ▼                                        │  │    │
│  ┌──────────────────────────────────────────────────────────┐  │  │    │
│  │              MongoDB Database                           │  │  │    │
│  ├──────────────────────────────────────────────────────────┤  │  │    │
│  │  Collections:                                            │  │  │    │
│  │  • users                                                 │  │  │    │
│  │  • sessions (optional)                                   │  │  │    │
│  │  • password_resets (for forgot password)                 │  │  │    │
│  └──────────────────────────────────────────────────────────┘  │  │    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          REGISTRATION FLOW                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User enters data in Register.jsx                                        │
│       ↓                                                                  │
│  Validate form (email, password match)                                   │
│       ↓                                                                  │
│  Call useAuth().register(fullname, username, email, password, repeat)   │
│       ↓                                                                  │
│  authService.register() → axios POST /api/auth/register                 │
│       ↓                                                                  │
│  Backend: Create user with role='user', hash password                   │
│       ↓                                                                  │
│  Backend: Generate JWT token (expires in 1 hour)                        │
│       ↓                                                                  │
│  Return { user, token }                                                  │
│       ↓                                                                  │
│  Zustand: setLogin(user, token)                                          │
│       ↓                                                                  │
│  Start auto-logout timer (1 hour)                                        │
│       ↓                                                                  │
│  Save to localStorage (persist middleware)                               │
│       ↓                                                                  │
│  Navigate to /user/dashboard                                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                            LOGIN FLOW                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User enters email and password in Login.jsx                             │
│       ↓                                                                  │
│  Call useAuth().login(email, password)                                   │
│       ↓                                                                  │
│  authService.login() → axios POST /api/auth/login                       │
│       ↓                                                                  │
│  Backend: Find user by email                                             │
│       ↓                                                                  │
│  Backend: Compare passwords (bcrypt)                                     │
│       ↓                                                                  │
│  Backend: Generate JWT token (expires in 1 hour)                         │
│       ↓                                                                  │
│  Return { user, token }                                                  │
│       ↓                                                                  │
│  Zustand: setLogin(user, token)                                          │
│       ↓                                                                  │
│  Start auto-logout timer (1 hour)                                        │
│       ↓                                                                  │
│  Save to localStorage (persist middleware)                               │
│       ↓                                                                  │
│  Navigate to /user/dashboard                                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         PROTECTED ROUTE FLOW                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User tries to access /user/dashboard                                    │
│       ↓                                                                  │
│  AppRouter renders route with <ProtectedRoute>                           │
│       ↓                                                                  │
│  ProtectedRoute checks:                                                  │
│    1. Is isAuthenticated === true?                                       │
│    2. Does user.role match requiredRole?                                 │
│       ↓                                                                  │
│  ✅ Both true → Render component                                         │
│  ❌ Not authenticated → Redirect to /login                               │
│  ❌ Wrong role → Redirect to /                                           │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          AUTO-LOGOUT FLOW                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User logs in                                                            │
│       ↓                                                                  │
│  Zustand calls startAutoLogout()                                         │
│       ↓                                                                  │
│  Timer set for 1 hour (3600000ms)                                        │
│       ↓                                                                  │
│  User makes API request (e.g., loads dashboard)                          │
│       ↓                                                                  │
│  Axios response interceptor calls refreshSession()                       │
│       ↓                                                                  │
│  Timer cleared and reset for another 1 hour                              │
│       ↓                                                                  │
│  (If user active: timer keeps resetting)                                 │
│  (If user inactive: timer expires after 1 hour)                          │
│       ↓                                                                  │
│  setAutoLogout() called:                                                 │
│    - Clear token from state                                              │
│    - Clear user data from state                                          │
│    - Clear from localStorage                                             │
│    - Clear timer                                                         │
│       ↓                                                                  │
│  Axios response interceptor detects 401                                  │
│       ↓                                                                  │
│  Redirect to /login                                                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                            LOGOUT FLOW                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User clicks Logout button in NavBar.jsx                                 │
│       ↓                                                                  │
│  Call useAuth().logout()                                                 │
│       ↓                                                                  │
│  authService.logout() → axios POST /api/auth/logout (optional)           │
│       ↓                                                                  │
│  Zustand: setLogout()                                                    │
│    - Clear token                                                         │
│    - Clear user                                                          │
│    - Cancel auto-logout timer                                            │
│    - Clear from localStorage                                             │
│       ↓                                                                  │
│  Navigate to home or login page                                          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                     API REQUEST WITH TOKEN FLOW                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Component calls: GET /api/auth/me or other protected endpoint           │
│       ↓                                                                  │
│  Axios request interceptor runs:                                         │
│    - Get token from Zustand store                                        │
│    - Add to header: Authorization: Bearer {token}                        │
│       ↓                                                                  │
│  Send HTTP request with token                                            │
│       ↓                                                                  │
│  Backend:                                                                │
│    - Extract token from header                                           │
│    - Verify signature with JWT_SECRET                                    │
│    - Check if expired                                                    │
│    - If valid: Extract user ID and proceed                               │
│    - If invalid/expired: Return 401                                      │
│       ↓                                                                  │
│  Response interceptor runs:                                              │
│    ✅ 200-299: refreshSession() (reset auto-logout timer)                │
│    ❌ 401: setLogout() and redirect to /login                            │
│       ↓                                                                  │
│  Component receives data or error                                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## State Management Flow

```
┌────────────────────────────────────────────────────────────────────────┐
│                      Zustand Auth Store State                          │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  INITIAL STATE:                                                        │
│  {                                                                     │
│    user: null,                                                         │
│    token: null,                                                        │
│    isAuthenticated: false,                                             │
│    isLoading: false,                                                   │
│    error: null,                                                        │
│    autoLogoutTimer: null                                               │
│  }                                                                     │
│                                                                        │
│  AFTER LOGIN/REGISTER:                                                 │
│  {                                                                     │
│    user: {                                                             │
│      id: "user_123",                                                   │
│      fullname: "John Doe",                                             │
│      username: "johndoe",                                              │
│      email: "john@example.com",                                        │
│      role: "user"                                                      │
│    },                                                                  │
│    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",                   │
│    isAuthenticated: true,                                              │
│    isLoading: false,                                                   │
│    error: null,                                                        │
│    autoLogoutTimer: <setTimeout object>                                │
│  }                                                                     │
│                                                                        │
│  DURING LOADING:                                                       │
│  {                                                                     │
│    ...previous state,                                                  │
│    isLoading: true                                                     │
│  }                                                                     │
│                                                                        │
│  ON ERROR:                                                             │
│  {                                                                     │
│    ...previous state,                                                  │
│    error: "Invalid credentials",                                       │
│    isLoading: false                                                    │
│  }                                                                     │
│                                                                        │
│  AFTER LOGOUT:                                                         │
│  {                                                                     │
│    user: null,                                                         │
│    token: null,                                                        │
│    isAuthenticated: false,                                             │
│    isLoading: false,                                                   │
│    error: null,                                                        │
│    autoLogoutTimer: null                                               │
│  }                                                                     │
│                                                                        │
│  📦 Persisted to localStorage as 'auth-storage'                        │
│  ♻️ Restored on page refresh                                           │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## Role-Based Access Control (RBAC) Decision Tree

```
                    ┌─────────────────────┐
                    │  User Accesses URL  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  Is Route Public?   │
                    └──────────┬──────────┘
                               │
                ┌──────────────┴──────────────┐
                │                            │
             YES│                            │NO
                │                            │
         ┌──────▼──────┐            ┌───────▼────────┐
         │  Render     │            │ Check: Is User │
         │  Component  │            │ Authenticated? │
         └─────────────┘            └───────┬────────┘
                                            │
                                  ┌─────────┴─────────┐
                                  │                   │
                              YES  │               NO  │
                                  │                   │
                           ┌──────▼────┐      ┌──────▼──────────┐
                           │Check User │      │Redirect to      │
                           │Role Match?│      │/login           │
                           └──────┬────┘      └─────────────────┘
                                  │
                          ┌───────┴────────┐
                          │                │
                      YES │            NO  │
                          │                │
                   ┌──────▼────┐   ┌──────▼──────────┐
                   │  Render   │   │Redirect to      │
                   │Component  │   │/ (Access Denied)│
                   └───────────┘   └─────────────────┘


Example Routes:
───────────────────────────────────────────────
Path                 Public? Role Required
───────────────────────────────────────────────
/                    YES     none
/login               YES     none
/register            YES     none
/about               YES     none
/contact             YES     none
/faqs                YES     none
───────────────────────────────────────────────
/user/dashboard      NO      user
/user/profile        NO      user
/user/deposit        NO      user
/user/withdraw       NO      user
───────────────────────────────────────────────
/admin/test1         NO      admin
/admin/test2         NO      admin
───────────────────────────────────────────────
```

---

## Session & Token Timeline

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           TOKEN TIMELINE                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  T+0min       Login/Register                                            │
│  │            ✓ Token created (valid for 1 hour)                        │
│  │            ✓ Auto-logout timer started (1 hour)                      │
│  │            ✓ Token stored in localStorage & Zustand                  │
│  │                                                                      │
│  T+5min       User makes API request                                    │
│  │            ✓ Axios interceptor adds Authorization header            │
│  │            ✓ Backend verifies token (OK, not expired)                │
│  │            ✓ Auto-logout timer reset (new 1 hour countdown)          │
│  │                                                                      │
│  T+15min      User navigates in app (no API calls)                      │
│  │            ✗ No API requests (timer not reset)                      │
│  │            ✗ Auto-logout timer continues (45 minutes left)           │
│  │                                                                      │
│  T+20min      User makes another API request                            │
│  │            ✓ Axios interceptor adds Authorization header            │
│  │            ✓ Backend verifies token (OK, not expired)                │
│  │            ✓ Auto-logout timer reset (new 1 hour countdown)          │
│  │                                                                      │
│  T+75min      Auto-logout timer expires (no activity)                  │
│  │            ✓ Timer callback triggers                                 │
│  │            ✓ setLogout() called                                      │
│  │            ✓ Token cleared from localStorage & Zustand               │
│  │            ✓ User redirected to /login                               │
│  │            ✗ Next API request → 401 error (token missing/invalid)   │
│  │                                                                      │
│  T+120min     Old token would expire on server anyway                  │
│  │            (Server-side expiry for security layer 2)                 │
│  │                                                                      │
└─────────────────────────────────────────────────────────────────────────┘

Key Points:
───────────
✓ Active users stay logged in indefinitely (timer keeps resetting)
✗ Inactive users logged out after 1 hour
✓ Security layer 1: Client-side auto-logout
✓ Security layer 2: Server-side token expiry
✓ Both mechanisms ensure sessions don't last forever
```

---

## Component Hierarchy with Auth

```
                        ┌──────────────┐
                        │   App.jsx    │
                        └──────┬───────┘
                               │
                        ┌──────▼───────┐
                        │ BrowserRouter│
                        └──────┬───────┘
                               │
                        ┌──────▼─────────────┐
                        │  AppRouter.jsx    │
                        └──────┬─────────────┘
                               │
                    ┌──────────┼──────────┐
                    │          │          │
            ┌───────▼──┐ ┌────▼──────┐ ┌▼──────────┐
            │ Public   │ │ User      │ │ Admin     │
            │ Routes   │ │ Layout    │ │ Layout    │
            └────┬─────┘ └────┬──────┘ └▼──────────┘
                 │            │           │
         ┌───────┴────┐  ┌────▼────┐  ┌──▼──────┐
         │  NavBar    │  │ Protected│  │Protected│
         │ (always)   │  │  Route   │  │ Route   │
         └────────────┘  │ (user)   │  │ (admin) │
                         └────┬─────┘  └──┬──────┘
                              │           │
                        ┌─────▼──────┐ ┌─▼────────┐
                        │ Dashboard  │ │Admin Test│
                        │ Uses useAuth│ │Uses useAuth
                        │for data    │ │for role  │
                        └────────────┘ └──────────┘

Every component can use useAuth() to:
  • Get user data
  • Get auth state
  • Call login/logout/register
  • Check user role
```

---

## Interceptor Flow Diagram

```
┌───────────────────────────────────────────────────────────────────────┐
│                    REQUEST INTERCEPTOR                               │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  Component calls:                                                    │
│  axios.get('/api/auth/me')                                           │
│       │                                                              │
│       ▼                                                              │
│  ┌─────────────────────────────────┐                               │
│  │  Request Interceptor Runs       │                               │
│  ├─────────────────────────────────┤                               │
│  │  1. Get token from Zustand      │                               │
│  │  2. If token exists:            │                               │
│  │     Add header:                 │                               │
│  │     Authorization: Bearer {token}                              │
│  │  3. Return config               │                               │
│  └─────────────────────────────────┘                               │
│       │                                                              │
│       ▼                                                              │
│  HTTP Request sent with token in header                             │
│                                                                       │
│                          ▼▼▼ BACKEND ▼▼▼                            │
│                                                                       │
│  Backend receives request                                            │
│  Backend verifies token and processes                                │
│                                                                       │
│                          ▼▼▼ RESPONSE ▼▼▼                           │
│                                                                       │
│       ▼                                                              │
│  ┌─────────────────────────────────┐                               │
│  │  Response Interceptor Runs      │                               │
│  ├─────────────────────────────────┤                               │
│  │  If status 200-299:             │                               │
│  │    • refreshSession()           │                               │
│  │    • Reset auto-logout timer    │                               │
│  │    • Return response            │                               │
│  │                                 │                               │
│  │  If status 401:                 │                               │
│  │    • Token expired/invalid      │                               │
│  │    • setLogout()                │                               │
│  │    • Redirect to /login         │                               │
│  │    • Reject promise             │                               │
│  │                                 │                               │
│  │  Other errors: Pass through     │                               │
│  └─────────────────────────────────┘                               │
│       │                                                              │
│       ▼                                                              │
│  Component receives data or error                                    │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```
